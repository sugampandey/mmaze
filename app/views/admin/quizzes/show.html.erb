<% title "Quiz" %>

<p>
  <strong>Name:</strong>
  <%= @quiz.name %>
</p>

<p>
  <strong>Columns:</strong>
  <%= @quiz.columns.collect(&:name).join(", ") if @quiz.columns %>
</p>

<p>
  <%= link_to "Edit", edit_admin_quiz_path(@quiz) %> |
  <%= link_to "Questions", admin_quiz_questions_path(@quiz) %> | 
  <%= link_to "Destroy", [:admin, @quiz], :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", admin_quizzes_path %>
</p>
